spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: f251d039e25de7
    password: c01f28a5e4b9d7
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
server:
  address: 0.0.0.0
  port: 8080

jwt:
  secret:
    key: "thisissecretkeyformypersonalspringbootapp"
  expiration: 86400000

cdn:
  upload-dir: /carauction/uploads/photos
  base-url: http://localhost:8080/cdn/photos