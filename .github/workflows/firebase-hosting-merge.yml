name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install and Build
        run: cd bidfe && npm install && npm run build
        env:
          VITE_API_URL: https://bid-your-car-867648614239.europe-west1.run.app/api/v1

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BIDFE_APP }}
          channelId: live
          projectId: bidfe-app
          entryPoint: ./bidfe